# 测试报告

## 1. 结论

- 阶段 1 测试结论：通过
- 阶段 2 当前测试结论：通过

## 2. 测试范围

- CLI
- API
- Persistence
- Workflow
- Upload
- Tool Registry
- PDF 解析
- 视频后处理链
- 鉴权
- RBAC
- 限流
- 幂等
- trace
- alert
- 运行时配置中心
- 角色注册
- 可切换执行协议
- 流式对话
- 请求路由中台最小版
- 异步任务深化

## 3. 自动化测试结果

最近一次结果：
- `101 tests, OK`

## 4. 阶段 2 已覆盖重点

- 统一鉴权失败路径
- 统一鉴权成功路径
- 最小 RBAC
  - `/auth/me`
  - `/auth/roles`
  - `/auth/subjects/{auth_subject}/roles`
  - viewer 无权写配置
- 限流 `429`
- POST 幂等重放
- trace 查询接口
- trace 与告警关联查询
- trace 聚合摘要查询
- 告警查询接口
- 运行时配置查询接口
- 运行时配置写入接口
- 数据库覆盖配置影响 workflow 有效配置
- 数据库覆盖配置影响 security 有效配置
- 数据库覆盖配置影响 recovery 有效配置
- 数据库覆盖配置影响 routing 有效配置
- 路由配置快照接口：`/routing/config`
- 路由决策查询：`/tasks/{task_id}/routes`、`/routes`
- 路由统计视图：`/routes/stats`
- 工作流角色注册默认值、数据库覆盖和环境变量覆盖
- 规划、执行、仲裁、批评、复核角色上下文注入 Prompt
- 多 Agent 辩论摘要落库与接口透出
- 仲裁摘要落库与接口透出
- 批评摘要落库与接口透出
- 复核结果落库与接口透出
- 可切换执行协议
  - `delegated`
  - `standard`
  - `standard` 模式下跳过 `debate / arbitration / critic`
- 流式对话输出
  - CLI 流式回答
  - `/chat/stream` SSE 流式事件
- 异步任务深化
  - `/tasks/submit`
  - `/tasks/runtime`
  - `/tasks/{task_id}/cancel`
  - `queued -> running -> completed/failed`
  - `task_events` 持久化与接口回查
  - `/tasks/{task_id}/events`
  - 任务查询回查异步状态

## 5. 覆盖重点

- 聊天主链路
- 上传落盘
- 任务与资产查询
- 失败任务落库
- PDF 正文提取
- 视频抽帧
- 视频抽音轨
- OCR / ASR 链
- trace / alert / task 联动查询
- route / task / trace 联动查询
- trace / task_events / tool_results / route_decisions / alerts 聚合查询

## 6. 未覆盖项

- 生产部署验证
- 分布式高并发
- 完整 RBAC 深化
- 真正的异步任务平台
- 可视化观测面板

## 7. 当前判断

当前测试已经足以支撑阶段 2 中后期的持续开发和提交，但还不足以证明阶段 3 的平台级可视化、高并发和运维能力。
